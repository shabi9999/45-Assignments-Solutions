"use strict";
//  Intentional Error
let array = ['shahbaz', 'momi', 'yasir', 6, 7, 29];
console.log(array[6]); // Intentional error: Arrays are zero-indexed, so index 5 is out of bounds.
console.log(array[0]); // Correcting the error by accessing a valid index.tsc
